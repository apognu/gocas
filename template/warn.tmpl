<!DOCTYPE html>
<html lang="en">
	<head>
		<link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.4/css/bootstrap.min.css">
		<title>Central Authentication Service</title>
	</head>
	<body>
		<div class="container">
			<div class="row">
				<div class="col-md-12">
					<h1>GoCAS - Single Sign-On</h1>
				</div>
			</div>

			<div class="panel panel-default">
  			<div class="panel-heading">Service authentication notification</div>
  			<div class="panel-body">
    			<p>You are about to be authenticated into <b>{{ .Service }}</b> as user <i>{{ .Username }}</i>.</p>
    			<p><a href="{{ .Url }}" class="btn btn-primary" role="button">Continue</a></p>
  			</div>
			</div>
		</div>
	</body>
</html>